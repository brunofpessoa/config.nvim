return {
  popup_border_style = "rounded",
  filesystem = {
    filtered_items = {
      hide_by_pattern = {
        ".node_modules",
      },

    },
    follow_current_file = {
      enabled = true,
      leave_dirs_open = true,
    },
    group_empty_dirs = false,
    buffers = {
      follow_current_file = {
        enabled = true,
        leave_dirs_open = true,
      },
      group_empty_dirs = false,
    },
  },
}
